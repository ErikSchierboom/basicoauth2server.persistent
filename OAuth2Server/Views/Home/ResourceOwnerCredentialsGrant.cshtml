@model OAuth2Server.ViewModels.Home.ResourceOwnerCredentialsGrantViewModel

@{
    ViewBag.Title = "Resource owner credentials grant tester";
}

<h2>Submit a resource owner credentials grant request</h2>

<section>
    <h2>Please enter the values you want to use.</h2>
    @using (Html.BeginForm(new { ReturnUrl = ViewBag.ReturnUrl })) {
        @Html.AntiForgeryToken()
        @Html.ValidationSummary(true)

        <fieldset>
            <legend>Resource owner credentials grant request form</legend>
            <ol>
                <li>
                    @Html.LabelFor(m => m.Username)
                    @Html.TextBoxFor(m => m.Username)
                    @Html.ValidationMessageFor(m => m.Username)
                </li>
                <li>
                    @Html.LabelFor(m => m.Password)
                    @Html.TextBoxFor(m => m.Password)
                    @Html.ValidationMessageFor(m => m.Password)
                </li>
                <li>
                    @Html.LabelFor(m => m.ClientId)
                    @Html.TextBoxFor(m => m.ClientId)
                    @Html.ValidationMessageFor(m => m.ClientId)
                </li>
                <li>
                    @Html.LabelFor(m => m.Scope)
                    @Html.TextBoxFor(m => m.Scope)
                    @Html.ValidationMessageFor(m => m.Scope)
                </li>
            </ol>
            <input type="submit" value="Submit request" />
        </fieldset>
    }
</section>

@{ Html.RenderPartial("AccessTokenResult");}